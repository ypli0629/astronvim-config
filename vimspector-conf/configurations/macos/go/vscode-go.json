{
  "configurations": {
    "Run Current Project": {
      "adapter": "vscode-go",
      "configuration": {
        "request": "launch",
        "program": "main.go",
        "cwd": "${cwd}",
        "mode": "debug"
      }
    },
    "Run Current Dir": {
      "adapter": "vscode-go",
      "configuration": {
        "request": "launch",
        "program": "${fileDirname}",
        "mode": "debug"
      }
    },
    "Attach Current Dir (Local)": {
      "adapter": "vscode-go",
      "configuration": {
        "request": "attach",
        "program": "${fileDirname}",
        "remotePath": "${fileDirname}",
        "cwd": "${fileDirname}",
        "mode": "local",
        "processId": "${PID}",
        "trace": "verbose"
      }
    },
    "Attach Current Dir (Remote)": {
      "adapter": "vscode-go",
      "configuration": {
        "request": "attach",
        "program": "${fileDirname}",
        "remotePath": "${fileDirname}",
        "cwd": "${fileDirname}",
        "mode": "remote",
        "trace": "verbose",
        "host": "localhost",
        "port": 4321
      }
    }
  }
}

